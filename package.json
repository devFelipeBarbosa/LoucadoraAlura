{
  "name": "locadora-loucadora",
  "version": "1.0.0",
  "description": "Sistema de locação de carros - Loucadora",
  "scripts": {
    "dev": "npm run backend:seed && concurrently \"npm run backend:dev\" \"npm run frontend:dev\"",
    "backend:dev": "cd backend && npm run dev",
    "frontend:dev": "cd frontend && npm run dev",
    "backend:start": "cd backend && npm start",
    "frontend:build": "cd frontend && npm run build",
    "backend:seed": "cd backend && npm run seed",
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install"
  },
  "keywords": [
    "locadora",
    "carros",
    "react",
    "nodejs",
    "sqlite"
  ],
  "author": "@aluraonline",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
